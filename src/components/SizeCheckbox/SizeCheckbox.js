import React from 'react';
import './SizeCheckbox.css';

const SizeCheckbox = () => {
    return (
        <div className={'sizeCheckbox'}>
            Test
        </div>
    );
};

export default SizeCheckbox;
        